{% import '@cgov_common/listing/list_item_macros.twig' as listItemMacros %}

{% if content.field_external_link[0] %}

  {# ###### Setup Title ####### #}
  {% if content.field_override_title[0] %}
    {% set title = content.field_override_title[0] %}
  {# {% else %}
    {% set title = content.field_external_link[0]["#node"].title.value %} #}
  {% endif %}

  {# ###### Setup the URL ###### #}
  {% set url = content.field_external_link[0]['#url'] %}

  {# ###### Setup View Mode ######## #}
  {% set view_mode = _context.view_mode %}

  {{ listItemMacros.listItem(title, null, null, null, url, view_mode) }}
{% endif %}
