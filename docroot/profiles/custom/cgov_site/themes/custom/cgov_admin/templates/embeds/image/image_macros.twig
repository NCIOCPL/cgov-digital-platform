{#
/**
 * @file
 * Theme macros for displaying article images.
 */
#}


{# Renders an article image  #}
{% macro articleImage(classes, image, enlarge_uri, credit, caption) %}

  {# Set up the classes for the figure #}
  {% set attr = create_attribute() %}
  {% set attr = attr.addClass(classes) %}
  {% set attr = attr.addClass('centered-set') %}

  <figure{{ attr }}>
      <div class="centered-element">
        {{ image }}
        {% if enlarge_uri[0] %}
          <a href="{{enlarge_uri[0]}}"
            target="_blank"
            class="article-image-enlarge no-resize"
          >{% trans %}Enlarge{% endtrans %}</a>
        {% endif %}
      </div>
      {# TODO: Handle empty captions, even when the captions are full of empty tags #}
      {% if caption|render|striptags|trim or credit  %}
        <figcaption>
          <div class="caption-container no-resize">
            {% if caption %}
              <p>{{caption}}</p>
            {% endif %}

            {% if credit %}
              <div class="image-photo-credit">{% trans %}Credit{% endtrans %}: {{ credit }}</div>
            {% endif %}
          </div>
        </figcaption>
      {% endif %}
  </figure>

{% endmacro %}
