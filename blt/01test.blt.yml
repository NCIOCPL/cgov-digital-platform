## This config will import features on a code update
## and not care if there are config differences.
cm:
  strategy: features
  allow-overrides: true
  features:
    allow-overrides: true
    bundle:
      - default
